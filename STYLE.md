# Style Rules

* Use `*` for unordered lists.
* Reference headings with bold text.
* Do not add decorative characters.
* Use backticks for code, commands, repository names, and filenames.
* Use fenced code blocks with a language specifier.

